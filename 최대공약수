#include<stdio.h>
int main(){
    int a, b;
    int c=0;
    scanf("%d %d", &a, &b);
    for(int i=0; i<=(a>b)?b:a; i++){
        if(a%i==0 && b%i==0){
            c+=i;
        }
    }
    return 0;
}