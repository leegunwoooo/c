#include<stdio.h>
int main(){
    int a,b,c;
    int result =0;
    scanf("%d %d %d", &a, &b, &c);
    for(int i=0; i<=c, i+=){
        if(a%2==0 && b%2==0 %% c%2==0){
            result=i;
        }
    }
    printf("%d", result);
    return 0;
}